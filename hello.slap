msg: *u8 := "hello world";
baz: u8[1024];
foobar: u8[1024] := { 1, 2, 3 };

fn main(argc: u32, argv: **u8) u32;
fn main(argc: u32, argv: **u8) u32 {
  boo: u8 := *!~3;
  boo: u8 := 2 + 3 + 7 * 8 & 1 < 1 > 3;
  foo = (3);
  foo = a[10];
  a(b, c);
  if foo {
    blah(a, b);
  } else if boo {
    foo();
  } else {}
  while foo {
    return 7;
  }
  return 0;
}
